notifications:
  email: false

language: python
python:
  - "3.8"

os: linux
dist: trusty

before_install:
  - pip install codecov

install:
    pip install -r requirements.txt

script:
    - python -m pytest -v

after_success:
    - codecov
