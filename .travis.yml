language: python
jobs:
  include:
    - name: "Python 3.7 on Xenial Linux"
      python: 3.7
    - name: "Python 3.7 on macOS"
      os: osx
      osx_image: xcode12
      language: shell
    - name: "Python 3.7 on Windows"
      os: windows
      language: shell
script:
  - python -m pytest -v
